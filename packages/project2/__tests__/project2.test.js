"use strict";

const project2 = require("..");

const assert = require("assert");

describe("project2", () => {
  it("needs tests", function() {
    const result = project2(12, 3);
    assert.equal(result, 16);
  });
});
